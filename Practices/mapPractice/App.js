import React from 'react'
import ReactDOM from 'react-dom'

export default function App(){
    const table = [1, "elephant", 2, 3, 4, joke = [1, 2, 3]]

    const tableElements = table.map(element=>{
        console.log(element);
    })
}

App();